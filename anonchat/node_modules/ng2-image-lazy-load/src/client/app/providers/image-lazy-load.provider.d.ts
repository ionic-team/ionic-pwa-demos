import { Http } from '@angular/http';
import { WebWorkerService } from './web-worker.provider';
export declare class ImageLazyLoaderService {
    private http;
    private worker;
    imageCache: any;
    constructor(http: Http, worker: WebWorkerService);
    load(url: string, headers?: any): Promise<any>;
    loadViaWorker(url: string, headers?: any): Promise<any>;
    loadViaHttp(url: string, headers?: any): Promise<any>;
}
