import { Observable } from 'rxjs/Observable';
import { NotificationStatic } from './notification';
export declare class NotificationPermission extends Observable<boolean> {
    constructor(notification?: NotificationStatic);
}
