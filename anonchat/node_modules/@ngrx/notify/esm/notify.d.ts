import 'rxjs/add/operator/cache';
import 'rxjs/add/operator/mergeMap';
import 'rxjs/add/observable/throw';
import { Observable } from 'rxjs/Observable';
import { NotificationStatic, NotificationOptions, NotificationInstance } from './notification';
export declare class Notify {
    private notificationConstructor;
    private globalOptions;
    private _permission$;
    constructor(notificationConstructor: NotificationStatic, globalOptions: NotificationOptions[], permission$: Observable<boolean>);
    requestPermission(): Observable<boolean>;
    private _createNotificationObservable(title, _options?);
    open(title: string, options?: NotificationOptions): Observable<NotificationInstance>;
}
